{% extends "base.html.twig" %}

{% block body %}

	<div class="container">
		<div class="row align-items-center">
			<div class="col-12">
				<div class="d-flex justify-content-between align-items-center">
					<h1>Editando el post</h1>
					<a href="{{ path('homepage') }}" class="btn btn-dark float-end">
						<i class="bi bi-arrow-left"></i>
						Volver
					</a>
				</div>
			</div>
		</div>
		<div class="row">
			{{ form(form) }}
			</div>
		</div>


	{% endblock %}
